- name: "WAYBAR"
  block:
  - name: "[INSTALL] - deps for waybar"
    community.general.pacman:
      name:
        - "waybar"
      state: present
    become: true
    become_user: 'root'
    become_method: 'sudo'

  - name: "[STOW] - waybar"
    shell: "stow -d {{ ansible_dotfiles }} -S waybar"
