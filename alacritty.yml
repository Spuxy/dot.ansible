- name: "[INSTALL] - Alacritty"
  community.general.pacman:
    name:
      - alacritty
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "[STOW] - Alacritty"
  shell: "stow -d {{ ansible_dotfiles }} -S alacritty"
