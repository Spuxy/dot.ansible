- name: "AGS"
  block:
  - name: "check if ags binary exists"
    stat:
      path: "/usr/bin/ags" # different name of bin
    register: ags_bin

  - name: "🧰 isntalling an ags"
    shell:
      cmd: "yay -S --answerclean Y --answerdiff N --noconfirm --noprogressbar aylurs-gtk-shell"
    become: true
    become_user: spuxy
    when: not ags_bin.stat.exists
