- name: "cava"
  block:
  - name: "📦 isntalling a packages"
    community.general.pacman:
      name:
        - "cava"
      executable: yay
      state: present

  - name: "🖨️ copying a dotfiles"
    shell: "stow -d {{ ansible_dotfiles }} -S cava"
