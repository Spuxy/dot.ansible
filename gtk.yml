- name: "GTK"
  block:
  - name: "INSTALL - {{ pkg }}"
    community.general.pacman:
      name:
        - "{{ pkg1 }}"
        - "{{ pkg2 }}"
      state: present
    become: true
    become_user: 'root'
    become_method: 'sudo'

  vars:
    pkg1: "gtk4"
    pkg2: "adwaita-icon-theme"
