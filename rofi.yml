- name: "rofi"
  block:
    - name: "📦 installing an package"
      community.general.pacman:
        name: "rofi-wayland"
        state: present
      become: true
      become_user: "root"
      become_method: "sudo"

    - name: "🖨️ copying an dotfiles"
      shell: "stow -d {{ ansible_dotfiles }} -S rofi"
