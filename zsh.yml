- name: "My Zsh shiet !"
  community.general.pacman:
    name:
      - zsh
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "change default shell to zsh"
  user:
    name: "{{ ansible_user }}"
    shell: /bin/zsh

# - name: "Check that the somefile.conf exists"
#   stat:
#     path: /home/spuxy/.oh-my-zsh
#   register: oh_my_stats

- name: "😻 isntalling pokemon-colorscript"
  shell:
    cmd: "yay -S --answerclean Y --answerdiff N --noconfirm --noprogressbar pokemon-colorscripts-git"
  become: true
  become_user: "{{ ansible_user }}"

- name: "copying a dotfiles"
  shell: "stow -d {{ ansible_dotfiles }} -S zsh"
