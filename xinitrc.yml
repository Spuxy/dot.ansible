- name: "Xinit"
  block:
  - name: "[INSTALL] - {{ pkg }}"
    community.general.pacman:
      name:
        - "{{ pkg }}"
      state: present
  - name: "[STOW] - {{ pkg }}"
    shell: "stow -d {{ ansible_dotfiles }} -S {{ dot_file }}"
  vars:
    pkg: "xinit-xsession"
    dot_file: "xinitrc"
