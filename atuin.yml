- name: "[INSTALL] - Atuin"
  community.general.pacman:
    name:
      - atuin
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "[STOW] - Atuin"
  shell: "stow -d {{ ansible_dotfiles }} -S atuin"
