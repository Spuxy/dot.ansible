- name: "tmux"
  block:
    - name: "📦 installing an package"
      community.general.pacman:
        name:
          - tmux 
        state: present

    - name: "🖨️ copying a dotfiles"
      shell: "stow -d {{ ansible_dotfiles }} -S tmux"
