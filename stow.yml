- name: "stow"
  block:
    - name: "📦 installing an package"
      community.general.pacman:
        name:
          - stow
        state: present
      become: true
      become_user: 'root'
      become_method: 'sudo'
