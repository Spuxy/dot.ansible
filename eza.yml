- name: "eza"
  block:
    - name: "📦 installing an package"
      community.general.pacman:
        name:
          - eza
        state: present
      become: true
      become_user: 'root'
      become_method: 'sudo'

- name: "🖨️ copying a dotfiles"
  shell: "stow -d {{ ansible_dotfiles }} -S eza"
