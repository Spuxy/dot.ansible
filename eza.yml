- name: "isntalling an eza"
  community.general.pacman:
    name:
      - eza
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "copying a dotfiles - eza"
  shell: "stow -d {{ ansible_dotfiles }} -S eza"
