- name: "[INSTALL] - mpd"
  community.general.pacman:
    name:
      - mpd
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "[STOW] - mpd"
  shell: "stow -d {{ ansible_dotfiles }} -S mpd"
