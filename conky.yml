- name: "[INSTALL] - conky"
  community.general.pacman:
    name:
      - conky
    state: present
  become: true
  become_user: 'root'
  become_method: 'sudo'

- name: "[STOW] - conky"
  shell: "stow -d {{ ansible_dotfiles }} -S conky"
